<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single" android:id="@+id/itens_menu_principal">
        <item android:id="@+id/fragmento_contatos"
            android:title="@string/fragmento_contatos"
            android:icon="@drawable/ic_face_white_24dp"
            android:checked="true" />
        <item android:id="@+id/fragmento_favoritos"
            android:title="@string/fragmento_favoritos"
            android:icon="@drawable/ic_favorite_white_24dp"
            />
    </group>
</menu>